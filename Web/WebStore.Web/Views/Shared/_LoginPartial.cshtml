@using WebStore.Common
@using WebStore.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


    <div class="user-panel">
        @if (this.SignInManager.IsSignedIn(this.User))
        {
            <div class="up-item">
                <i class="flaticon-profile"></i>
                <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @(this.User.Identity.Name)!</a>
            </div>
            if (this.User.IsInRole(GlobalConstants.AdministratorRoleName))
            {
                <div class="up-item">
                    <a asp-area="Administration" asp-controller="Dashboard" asp-action="Index" title="Admin">Dashboard</a>
                </div>
            }
            else
            {
                    <vc:shopping-cart></vc:shopping-cart>
                
            }
            <div class="up-item">
                <form  asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@(this.Url.Action("Index", "Home", new { area = string.Empty }))">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </div>

        }
        else
        {
            <div class="up-item">
                <i class="flaticon-profile"></i>
                <a asp-area="Identity" asp-page="/Account/Login">SignIn</a> or <a asp-area="Identity" asp-page="/Account/Register">Create Account</a>
            </div>
            <div class="up-item">
                <div class="shopping-card">
                    <i class="flaticon-bag"></i>
                    <span>0</span>
                </div>
                <a asp-area="" asp-controller="ShoppingCartItems" asp-action="Index">Shopping Cart</a>
            </div>

        }

    </div>


@*<ul class="navbar-nav">
        @if (this.SignInManager.IsSignedIn(this.User))
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @(this.User.Identity.Name)!</a>
            </li>
            if (this.User.IsInRole(GlobalConstants.AdministratorRoleName))
            {
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="Administration" asp-controller="Dashboard" asp-action="Index" title="Admin">Admin</a>
                </li>
            }
            <li class="nav-item">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@(this.Url.Action("Index", "Home", new { area = string.Empty }))">
                    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>
        }
    </ul>*@
