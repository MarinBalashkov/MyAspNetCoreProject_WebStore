@model WebStore.Web.ViewModels.ViewComponents.NavBarViewModel


@if (Model.NavBarItems.Any())
{
    @foreach (var item in Model.NavBarItems)
    {
        <li>
            <a asp-area="" asp-controller="Products" asp-action="Index" asp-route-ParentCategoryName="@item.ParentCategory.Name">@item.ParentCategory.Name</a>
            <ul class="sub-menu">
                @if (item.SubCategories.Any())
                {
                    @foreach (var subCategory in item.SubCategories)
                    {
                        <li><a asp-area="" asp-controller="Products" asp-action="Index" asp-route-ParentCategoryName="@item.ParentCategory.Name" asp-route-ChildCategoryName="@subCategory.Name">@subCategory.Name</a></li>
                    }
                }

            </ul>
        </li>
    }
}


